num a = 1;

while 1 > 0 do
    num b = a + 3;
    if 1 == 1 then
        num c = a + b + 1;
    end
    num g = a + b + c; // c it out of scope
end

num d = a + b; // b is out of scope
num e = a + c; // c is out of scope
num f = a;