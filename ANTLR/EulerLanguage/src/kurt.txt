num index = 0;
mtx A = {0.95, 0; 0, 1};
vec x = <12, 0>;
mtx B = {1.1, 5};
vec u = <1>;

while index < 50 do
    if x[0] < 20 then
        u = <1>;
    else if x[0] > 20 then
        u = <0>;
    end
    x = (x * A) + (u * B);
    print "temp: " + x[0] + "price: " + x[1];
end